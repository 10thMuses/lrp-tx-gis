# Daily Auto-Refresh — 2026-07-30

**Branch:** `refinement-daily-refresh-2026-07-30`
**Run time:** 2026-07-30T06:00–06:20 UTC (approx)

---

## Step 2 — Preflight Reachability

| Endpoint | HTTP Code | Class |
|---|---|---|
| mft.rrc.texas.gov | 302 | ok (3xx redirect) |
| www.ercot.com | 200 | ok |

> Note: RRC MFT returned 302 (not the expected 403). Data fetches proceeded and SUCCEEDED — atypical for this cloud egress environment.

---

## Step 3 — Refresh Results

| Step | Script(s) | Exit | Result |
|---|---|---|---|
| wells fetch | scripts/fetch_rrc.py wells | 0 | ok — 367 MB (dbf900.txt.gz) |
| wells parse | scripts/parse_rrc.py wells | 0 | ok — 99,698 rows → data/wells_permian6.csv (gitignored) |
| permits fetch | scripts/fetch_rrc.py permits | 0 | ok — 107 EOM snapshots fetched |
| permits parse | scripts/parse_rrc.py permits | 0 | ok — 28,842 rows → data/permits_permian6.csv (gitignored) |
| ercot geocode | scripts/geocode_ercot_queue.py | 0 | ok — 1778 queue rows; 479 non-centroid (26.9%); match rate 27% vs 60% target; log updated, no combined_* change |
| abatements | scripts/scrape_abatements.py | 0 | ok — Pecos: 17 hits, Reeves: 0 hits → 12 rows (diagnostic only; NOT committed) |

**Module installs required this run:** rapidfuzz, beautifulsoup4, pyshp, shapely, cairosvg (missing from bootstrap; added via pip3 --break-system-packages).

---

## Step 4 — Change Detection

`git status --porcelain` output:
```
 M outputs/refresh/_geocode_ercot_log.txt
```

`combined_points.csv` — **no change**
`combined_geoms.geojson` — **no change**

**Result: NO-OP.** No deployable combined_* diff.

### Architecture note (flagged for operator review)

The task contract states that `data/wells_permian6.csv` and `data/permits_permian6.csv` "reach the map only via `combined_points.csv` after `python3 build.py`." However, in the current `layers.yaml`, both layers reference their own standalone gitignored CSV files directly (`file: data/wells_permian6.csv`, `file: data/permits_permian6.csv`) and are NOT combined_points.csv-resident. Today wells and permits data WAS successfully fetched and parsed (RRC MFT was accessible at 302), but the updated gitignored CSVs cannot trigger a combined_* diff. A manual operator-triggered `python3 build.py && bash scripts/deploy.sh --rebuild` would incorporate today's refreshed wells/permits data into the deployed map. Consider whether the daily auto-refresh should run build.py and check for PMTiles-level changes when RRC sources are reachable.

### Ercot geocoder match rate

Non-centroid match rate: 27.0% (solar+wind+battery). Acceptance target: ≥60%. Match rate below target — existing geocoded positions retained, no regression introduced (ercot geocoder only upgrades or keeps coordinates). This rate is consistent with prior runs.

---

## Step 5 — Build + Deploy

**SKIPPED** — no combined_* change (NO-OP path).

---

## Step 6b — Close-out

`bash scripts/close-out.sh refinement-daily-refresh-2026-07-30 none "daily auto-refresh 2026-07-30"`

---

## Step 7 — Summary

`[daily-refresh 2026-07-30] preflight=rrc:ok(302),ercot:ok(200) steps=wells:ok(99698rows),permits:ok(28842rows),ercot:ok(27%match-no-change),abate:ok(12hits) rendered_change=none result=NO-OP prod already current report=outputs/refresh/daily/2026-07-30.md`
