# Daily Auto-Refresh Report — 2026-08-03

Run started: 2026-08-03T06:05 UTC  
Branch: `refinement-daily-refresh-2026-08-03`

---

## Step 2 — Reachability Preflight

| Endpoint | HTTP code | Class |
|---|---|---|
| RRC MFT (mft.rrc.texas.gov) | 302 | ok |
| ERCOT (www.ercot.com) | 200 | ok |

Note: Both endpoints classified as `ok` (2xx/3xx) — RRC MFT returned 302 redirect (reachable from cloud egress today, atypical).

---

## Step 3 — Data Refresh Steps

| Step | Class | Exit | Rows / Result | Notes |
|---|---|---|---|---|
| a. wells fetch | ok | 0 | 367.6 MB downloaded | `dbf900.txt.gz` written to `data/rrc_raw/` |
| a. wells parse | ok | 0 | 99,711 rows → `data/wells_permian6.csv` | 11.10 MB output |
| b. permits fetch | FAILED | 1 | Aborted at ~60/108 EOM snapshots | `ConnectionResetError(104, 'Connection reset by peer')` at POST to mft.rrc.texas.gov |
| b. permits parse | SKIPPED | — | Skipped (fetch failed) | — |
| c. ercot geocoding | ok | 0 | 479/1778 non-centroid (26.9%) | Log updated; match rate 27% vs 60% target |
| d. abatements | ok | 0 | 12 rows (Pecos=17 hits, Reeves=0) | `data/abatements/abatement_hits_20260803_060859.csv` (diagnostic only, not staged) |

### Permits error tail:
```
requests.exceptions.ConnectionError: ('Connection aborted.',
ConnectionResetError(104, 'Connection reset by peer'))
```

---

## Step 4 — Change Detection

`git status --porcelain`: clean (no changes to `combined_points.csv` or `combined_geoms.geojson`).

**Result: NO-OP** — data pipeline steps do not write to combined_* files directly. Wells parsed successfully (99,711 rows in `data/wells_permian6.csv`, gitignored) but not merged. A separate `python3 build.py merge wells data/wells_permian6.csv` would be needed to push this to the map.

**Notable:** RRC MFT was reachable from cloud egress today (302 redirect). Wells data downloaded and parsed successfully — this is atypical. Permits was throttled/connection-reset at 60/108 EOM snapshots.

---

## Step 5 — Build + Deploy

Skipped (no combined_* change in step 4).

---

## Step 6b — Close-out

Branch will be merged via `bash scripts/close-out.sh`.

---

## Step 7 — Summary

`[daily-refresh 2026-08-03] preflight=rrc:ok,ercot:ok steps=wells:ok(99711rows),permits:fail(ConnectionReset@60/108),ercot:ok(479geocoded),abate:ok(12hits) rendered_change=none result=NO-OP prod already current report=outputs/refresh/daily/2026-08-03.md`
