# Secrets — live only in Project Knowledge at /mnt/project/CREDENTIALS.md
CREDENTIALS.md

# Local environment files (Claude Code)
.env
.env.local
.env.*.local

# Build artifacts
dist/
*.pmtiles

# Python
__pycache__/
*.pyc
.venv/
venv/

# Scratch / tmp
tmp*/

# Large upstream archives (re-fetchable)
outputs/refresh/tl_*.zip
*.tmp

# RRC raw downloads + parsed county-filtered CSVs (re-fetchable; ~hundreds of MB)
data/rrc_raw/
data/wells_permian6.csv
data/permits_permian6.csv
.DS_Store
Thumbs.db
desktop.ini

# Local user-data mounts
/mnt/user-data/

# Claude Code session artifacts
.claude/

# Daily abatements probe outputs (scripts/scrape_abatements.py) are diagnostic
# artifacts, NOT a layer source. tax_abatements is fed only by the pinned
# snapshot below via scripts/transform_abatements.py. Ignore the daily probe
# files so the unattended daily-refresh routine cannot commit them.
data/abatements/abatement_hits_*.csv
!data/abatements/abatement_hits_20260424_092810.csv

# RRC W-1 1976-2017 permits backfill scratch + checkpoints (re-derivable from
# scripts/scrape_rrc_w1*.py; merged into permits_permian6 at parse time, not a
# committed layer source). Ignored so the unattended daily-refresh routine and
# accidental `git add` cannot commit them.
outputs/refresh/rrc_w1_*
data/fracfocus/FracFocusCSV.zip
