# Settled decisions — pointer

Canonical list: `ARCHITECTURE.md` settled-decisions section. Do not re-litigate without strong reason.

- MapLibre + PMTiles + tippecanoe. Not Leaflet, not Mapbox GL JS proper.
- Netlify hosting; MCP -> CLI proxy deploy path. REST API deprecated for this site.
- Single-page app, no build framework, hand-edited `build_template.html`.
- Combined data files with `layer_id` tag; standalone only when size or schema demands.
- `main` canonical; `refinement-<slug>` feature branches; direct merge, no PRs.
- Flat repo-root layout.
- Grid Wire: light theme only, no dark background shading behind text (operator directive 2026-07-21).
- Grid Wire: crude and natural gas never conflated; water claims district-specific; comps never averaged across entitlement stages.
- Grid Wire: email drafts are never auto-sent.
- ERCOT Batch Zero financial security is $50k/MW non-refundable fee per 16 TAC §25.194 (supersedes the $100k+$100k stack carried in Vols 19-20).
